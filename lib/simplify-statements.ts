import { AndBuilder } from 'logic-statement-ts';
import { Path } from './types';

function AlternativeSimplify({ path }: { type: 'alternate', path: Path[] }, builder: AndBuilder<Path> = new AndBuilder<Path>()) {
  builder.
  
  
  for (const p of path) {
    p.type
  }
}
